
<div class="sign-in-form">


  <h5 class="sign-in-title">"Education is the kindling of a flame, not the filling of a vessel" - Socrates</h5>

  <!-- NOT SURE WHY @user isn't working but User.new is??  -->
  <%= form_for User.new, url: "/sessions", method: "post" do |f| %>
    <%#= render "shared/flash_notice_signin" -%>
    <div>
      <%= f.label :email %>
      <%= f.text_field :email, placeholder: "Email: " %>
    </div>
    <div>
      <%= f.label :password %>
      <%= f.password_field :password, placeholder: "Password: " %>
    </div>

    <%= f.submit "Sign In" %>

  <% end %>

</div>
