<% provide(:title, @user.first_name) %>

<h1>
  Hi, <%= @user.first_name %>!
</h1>

<div class="col-md-8">
  <% if @user.lists.any? %>
    <h3>Lists (<%= @user.lists.count %>)</h3>
    <ul class="lists">
      <%= render @lists %>
    </ul>

  <% end %>
</div>
